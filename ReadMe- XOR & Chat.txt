Chat Program:

user@latech:~/$ python chat.py <IP> <PORT>
[Connect to server}
...Print some covert message.


XOR Program:

user@latech:~/$ python xor.py < <cipherText>  	--> when the ciphertext is already xor'd with key. 

user@latech:~/$ python xor.py < plainText > cipherText  --> some plainText(unecrypted) xor'd with the key, then stored in cipherText.